#ifndef RAIDAConfig_h
#define RAIDAConfig_h 1

// version macros
#define RAIDA_MAJOR_VERSION @RAIDA_VERSION_MAJOR@
#define RAIDA_MINOR_VERSION @RAIDA_VERSION_MINOR@
#define RAIDA_PATCH_LEVEL @RAIDA_VERSION_PATCH@
//#define RAIDA_VERSION_GE( MAJV , MINV , PLEV)  ( (  RAIDA_MAJOR_VERSION > MAJV ) || ( (RAIDA_MAJOR_VERSION==MAJV) && ( RAIDA_MINOR_VERSION > MINV ) ) || ( (RAIDA_MAJOR_VERSION==MAJV) && ( RAIDA_MINOR_VERSION == MINV ) && ( RAIDA_PATCH_LEVEL >= PLEV ) ))
#define RAIDA_VERSION_GE( MAJV , MINV )  ( (  RAIDA_MAJOR_VERSION  > MAJV ) || ( (RAIDA_MAJOR_VERSION==MAJV) && ( RAIDA_MINOR_VERSION >= MINV ) ) )

#endif // RAIDAConfig_h
